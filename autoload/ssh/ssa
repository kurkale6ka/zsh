ssh-add -l >/dev/null
# Error connecting to agent
if [[ $? == 2 ]]
then
   return 1
elif [[ $? == 1 ]]
then
   print -P '%F{45}id_rsa%f:'
   ssh-add
fi

# Add non default keys
local key k
for k in ~/.ssh/(^id_rsa).pub
do
   key=${k%.pub}
   print -P "%F{45}${key##*/}%f:"
   ssh-add $key
done
