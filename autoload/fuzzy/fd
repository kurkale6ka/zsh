# Fuzzy cd under the current directory, excluding dot folders
# usage fd [pattern]

local dir

if (($#))
then
   dir=$(print -l **/*${(j.*.)@}*(/) | fzf -0 -1 +m)
else
   dir=$(print -l **/*(/) | fzf -0 -1 +m)
fi

[[ -d $dir ]] && cd -- $dir
