# Fuzzy stuff using fzf

## fuzzy cd based on visited locations only (bookmarks)

* .zsh/autoload/fuzzy/c
* .zsh/autoload/fuzzy/update_marks
* `chpwd_functions+=(update_marks)` in `~/.zshrc`

## fuzzy cd to anywhere + fuzzy file opening with nvim

* .zsh/autoload/fuzzy/cf
* .zsh/autoload/fuzzy/vf
